function sum(a,b){
return a+b
}

const result = sum(20,30)
console.log("Sum=" + result)

//To run a js code written in notepad, save the file, go to the folder where the file is saved and then write "node <filename.js>" in the command prompt. For example, if the file name is sum.js, you would write "node sum.js" to execute the code.